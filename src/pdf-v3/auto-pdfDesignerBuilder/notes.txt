//one column mode:
Fill pages top‑to‑bottom in a single flow including first page below cover, respecting your padding.

Images: cap max‑height (e.g. 400 px) and preserve aspect ratio. to fix too much white space on a page. 

Side‑by‑side images: if two images occur consecutively, wrap them in a flex container to display them inline.

futher info on the side by side
if we have title -> image or 

Small text blocks: auto‑group multiple paragraphs or treat them as a “cluster” to avoid orphan pages.